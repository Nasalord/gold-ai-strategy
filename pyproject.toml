[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gold-ai-strategy"
version = "0.1.0"
description = "Public paper-only systematic strategy research and validation lab"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["ai_investing_lab*"]
